<%= form_with(model: job) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
        <% job.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
    <div class="col">

  <div class="form-group">
    <%= form.label :consultant_id, "Consultant Name" %>
    <%= form.collection_select(:consultant_id, Consultant.all, :id, :name, prompt: true) %>
  </div>

  <div class="form-group">
    <%= form.label :client_id, "Client Name"  %>
    <%= form.collection_select(:client_id, Client.all, :id, :name, prompt: true) %>
  </div>
</div>
</div>



  <div class="form-group">
    <%= form.label :location %>
    <%= form.text_form-group :location %>
  </div>
  <div class="row">
      <div class="col">
  <div class="form-group">
    <%= form.label :start_time %>
    <%= form.datetime_select :start_time %>
  </div>

  <div class="form-group">
    <%= form.label :end_time %>
    <%= form.datetime_select :end_time %>
  </div>
</div>
</div>
  <div class="form-group">
    <%= form.label :material %>
    <%= form.text_form-group :material %>
  </div>

  <div class="form-group">
    <%= form.label :cost %>
    <%= form.text_form-group :cost %>
  </div>

  <div class="form-group">
    <%= form.label :notes %>
    <%= form.text_area :notes %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
