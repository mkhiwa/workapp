<p id="notice"><%= notice %></p>

<h1>Jobs</h1>

<table>
  <thead>
    <tr>
      <th>Consultant</th>
      <th>Client</th>
      <th>Location</th>
      <th>Start time</th>
      <th>End time</th>
      <th>Material</th>
      <th>Cost</th>
      <th>Notes</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @jobs.each do |job| %>
      <tr>
        <td><%= job.consultant_id %></td>
        <td><%= job.client_id %></td>
        <td><%= job.location %></td>
        <td><%= job.start_time %></td>
        <td><%= job.end_time %></td>
        <td><%= job.material %></td>
        <td><%= job.cost %></td>
        <td><%= job.notes %></td>
        <td><%= link_to 'Show', job %></td>
        <td><%= link_to 'Edit', edit_job_path(job) %></td>
        <td><%= link_to 'Destroy', job, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Job', new_job_path %>
